// Generated by webapp-test-orchestrator.
//
// During the exploratory loop, Claude:
//   1. Generates Page Object Models in tests/pages/<Name>Page.ts
//   2. Edits THIS file to extend the test fixture so specs receive POMs as args
//
// Example after first POM is generated (LoginPage):
//
//   import { LoginPage } from '../pages/LoginPage';
//
//   export const test = base.extend<{ loginPage: LoginPage }>({
//     loginPage: async ({ page }, use) => use(new LoginPage(page)),
//   });
//
// Spec then receives it as a fixture:
//
//   test('logs in', async ({ loginPage }) => { await loginPage.goto(); ... });

import { test as base } from '@playwright/test';

export const test = base;
export { expect } from '@playwright/test';
